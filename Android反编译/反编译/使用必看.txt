反编译和回编译2种,新增签名，都需要java环境。

1.反编译必须把apk文件放在同目录。
反编译完成后会在目录内生成一名为Yelp的文件夹，那就是反编译的文件。

2.编译 也就是回编译。
编译后会生成文件“Yelp\已编译”目录内，回编译的文件需要重新签名才能使用。

3.签名.需要把apk文件命名为Yelp.apk，并且放在同目录（也就是和APKtool反编译文件同一个目录）。


本程序只能单个反编译，编译完成后清理在反编译！

Www.YYsec.Com  By:Tender  QQ群：92373992  欢迎加入！